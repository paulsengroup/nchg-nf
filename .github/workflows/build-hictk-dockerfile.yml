# Copyright (c) 2025 Roberto Rossini (roberros@uio.no)
# SPDX-License-Identifier: MIT

name: Build hictk Dockerfile

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/build-hictk-dockerfile.yml"
      - ".github/workflows/build-dockerfile.yml"
      - "containers/hictk__*.Dockerfile"
  pull_request:
    paths:
      - ".github/workflows/build-hictk-dockerfile.yml"
      - ".github/workflows/build-dockerfile.yml"
      - "containers/hictk__*.Dockerfile"

# https://stackoverflow.com/a/72408109
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build-hictk-dockerfile:
    name: Build hictk Dockerfile
    uses: paulsengroup/nchg-nf/.github/workflows/build-dockerfile.yml@main
    with:
      dockerfile-glob: "containers/hictk__*.Dockerfile"

    permissions:
      contents: read
      packages: write
